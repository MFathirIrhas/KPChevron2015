@model IEnumerable<KPChevron2015.Models.Role>

@{
    ViewBag.Title = "Index";
}

<h2>Role</h2>

@*<p>
    @Html.ActionLink("Create New", "Create")
</p>*@
<p>
    <a href='@Url.Action("Create", "Roles")'>
        <input type='button' value='&#43 Add New Role' />
    </a>
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.RoleName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.RoleDesc)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.UpdateBy)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.UpdateDate)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.RoleName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.RoleDesc)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.UpdateBy)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.UpdateDate)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.RoleID }) |
            @*@Html.ActionLink("Details", "Details", new { id=item.RoleID }) |*@
            @Html.ActionLink("Delete", "Delete", new { id=item.RoleID })
        </td>
    </tr>
}

</table>
<br />
@Html.ActionLink("Back", "Index", "Admins")