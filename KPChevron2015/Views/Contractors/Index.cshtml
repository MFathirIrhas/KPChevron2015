@model IEnumerable<KPChevron2015.Models.Contractor>

@{
    ViewBag.Title = "Index";
}

<h2>Contractor</h2>

@*<p>
    @Html.ActionLink("Create New", "Create")
</p>*@
<p>
    <a href='@Url.Action("Create", "Contractors")'>
        <input type='button' value='&#43 Add New Contractor' />
    </a>
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.ContractorName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Remark)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.ContractorName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Remark)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ContractorID }) |
            @*@Html.ActionLink("Details", "Details", new { id=item.ContractorID }) |*@
            @Html.ActionLink("Delete", "Delete", new { id=item.ContractorID })
        </td>
    </tr>
}

</table>
<br />
@Html.ActionLink("Back", "Index", "Admins")